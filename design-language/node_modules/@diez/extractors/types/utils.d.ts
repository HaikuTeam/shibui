import { Reporters } from '@diez/extractors-core';
import { AssetFolder } from '@diez/generation';
import { ImageFormats } from './api';
/**
 * Splits an array into chunks of `chunkSize` elements.
 * @ignore
 */
export declare const chunk: <T>(arr: T[], chunkSize: number) => T[][];
/**
 * Empties `basePath` and creates the provided `folders` inside.
 * @ignore
 */
export declare const createFolders: (basePath: string, folders: Iterable<AssetFolder>) => Promise<void[]>;
/**
 * Escapes a command to be safe for shell usage.
 * @todo - Move this to @diez/cli-core.
 * @ignore
 */
export declare const escapeShell: (cmd: string) => string;
/**
 * Adjusts gamma of PNG files to make them look consistent across browsers.
 */
export declare const adjustImageGamma: (base64data: string, imageFormat: ImageFormats) => string;
/**
 * Generates a random file name and path in the OS temporary directory.
 * @ignore
 */
export declare const generateRandomFilePath: (extension?: string) => string;
/**
 * Fixes the gamma values of SVGs in a directory.
 */
export declare const fixGammaOfSVGs: (directory: string) => void;
/**
 * Reporters singleton used by extractors.
 * @ignore
 */
export declare const cliReporters: Reporters;
//# sourceMappingURL=utils.d.ts.map