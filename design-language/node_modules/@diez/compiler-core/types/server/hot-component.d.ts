declare global {
    interface Window {
        componentName: string;
        component: object;
        __resourceQuery: string;
    }
}
import { Patcher } from '@diez/engine';
/**
 * Activates a hot component with the provided patcher.
 */
export declare const activate: (patcher: Patcher) => Promise<void>;
//# sourceMappingURL=hot-component.d.ts.map