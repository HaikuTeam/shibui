import { Patcher } from '@diez/engine';
declare global {
    interface Window {
        webkit: {
            messageHandlers: {
                patch: {
                    postMessage: Patcher;
                };
            };
        };
    }
}
//# sourceMappingURL=ios.component.d.ts.map