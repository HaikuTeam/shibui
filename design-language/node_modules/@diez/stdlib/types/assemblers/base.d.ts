/// <reference types="node" />
import { Assembler, TargetOutput } from '@diez/compiler-core';
/**
 * A base class our assembler implementations should inherit from.
 */
export declare abstract class BaseAssembler<T extends TargetOutput> implements Assembler<T> {
    readonly output: T;
    constructor(output: T);
    abstract addCoreFiles(): Promise<void | void[]>;
    writeFile(destinationPath: string, contents: string | Buffer): Promise<void>;
    copyFile(sourcePath: string, destinationPath: string): Promise<void>;
}
//# sourceMappingURL=base.d.ts.map