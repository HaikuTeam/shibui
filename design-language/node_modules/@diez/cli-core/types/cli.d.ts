declare global {
    namespace NodeJS {
        interface Global {
            doNotTrack: boolean;
            analyticsUuid: string;
        }
    }
}
/**
 * Bootstraps all available CLI commands based on local package dependencies by scanning for `"diez"` keys
 * in `package.json` and `.diezrc` files.
 */
export declare const bootstrap: (rootPackageName?: string, bootstrapRoot?: string | undefined) => Promise<void>;
/**
 * Starts the CLI program.
 * @ignore
 */
export declare const run: (bootstrapRoot?: string | undefined) => Promise<void>;
//# sourceMappingURL=cli.d.ts.map